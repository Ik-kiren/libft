#include "libft.h"
#include <limits.h>

int main()
{
    /*char p[4] = "tes";
    ft_bzero(p, 3);
    printf("%s", p);


    char str[40] = "    je   dois tester des trucs   ";
    char **p;
    int i = 0;
    p = ft_split(str, ' ');
    while (p[i] != NULL)
    {
        printf("%s\n", p[i]);
        i++;
    }

    char *str = ft_itoa(INT_MIN);
    printf("%d", INT_MIN);*/
    
}